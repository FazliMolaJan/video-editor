<?xml version="1.0" encoding="utf-8"?>
<android.support.design.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:fab="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.luckstro.videooverlay.MainActivity">


        <TextView
            android:id="@+id/entity_grid_view_empty_text_view"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/no_projects"
            android:textStyle="italic"
            android:layout_gravity="center|center_vertical"/>

        <GridView
            android:id="@+id/entity_grid_view"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:columnWidth="128dp"
            android:numColumns="3"
            android:visibility="gone"></GridView>

        <com.getbase.floatingactionbutton.FloatingActionsMenu
            android:id="@+id/multiple_action_menu"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom|end"
            android:layout_margin="@dimen/fab_margin"
            app:srcCompat="@android:drawable/ic_input_add">

                <com.getbase.floatingactionbutton.FloatingActionButton
                    android:id="@+id/multiple_action_menu_new_team"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:src="@drawable/team_icon_small"
                    fab:fab_colorNormal="@color/white"
                    fab:fab_title="New Team"
                    fab:fab_colorPressed="@color/white"/>

                <com.getbase.floatingactionbutton.FloatingActionButton
                    android:id="@+id/multiple_action_menu_new_player"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:src="@drawable/player_icon"
                    fab:fab_colorNormal="@color/white"
                    fab:fab_title="New Team"
                    fab:fab_colorPressed="@color/white"/>

                <com.getbase.floatingactionbutton.FloatingActionButton
                    android:id="@+id/multiple_action_menu_new_project"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:src="@drawable/movie_icon"
                    fab:fab_colorNormal="@color/white"
                    fab:fab_title="New Team"
                    fab:fab_colorPressed="@color/white"/>

        </com.getbase.floatingactionbutton.FloatingActionsMenu>
</android.support.design.widget.CoordinatorLayout>